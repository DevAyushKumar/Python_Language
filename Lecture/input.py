''' TAKING INPUT FROM USER
In python, we can take input directly from the user by using the input() function. This input function gives a return value as string/character hence we have to pass that into a variable. 

Syntax:
variable = input()

But input function returns the value as a string. Hence we have to typecaste them whenever required to another datatype'''

a=int(input("As an integer"))
b=float(input("As a float"))
b=input("As a string")